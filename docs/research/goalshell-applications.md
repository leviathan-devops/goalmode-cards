# Research corpus — Goal Shell applications (v1)
Contract: survey the applications of /goal-as-kernel + GM cards; honest, comparable, logged. Baseline: v1 seed architecture (MIMOCODE lineage). Method: session evidence (fetched, anchored) + marked [TK] external framing (fetch attempts 404'd — never cited as fetched).
Applications inventory (each = a card + slot set):
A1 repo build (proven: this repo, cards/GOAL_REPO_BUILD_v1.md) — contract: artifacts + batteries + ship.
A2 feature build w/ spec (worked: BUILD_TETRIS_v1.md) — SPEC anchors + TDD slots + panels.
A3 substrate deploy (proven: deploy-proof.mjs clean target) — DEPLOY + in-target smoke.
A4 research pipeline (wired: gm-research slot; super-research harness) — topic-survey + experiment modes, TSV logs.
A5 cadence ops (wired: /loop) — keepalive, nightly cold §0 drift checks.
A6 red-team validation (wired: gm-panels) — adversarial + 3-juror on testing data.
Log (TSV discipline): attempts and failures are kept in the build's battery history (README bug ledger rows 1-7) — RED runs, false-fail validator, deploy gap, firewall adaptation.
Open: metric-reduction for A4 (which numeric metric defines research quality); panels-in-runtime proof (gm-panels via the workflow tool on a live substrate).
